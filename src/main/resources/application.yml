micronaut:
  application:
    name: meta-server
  server:
    port: 80
  router:
    static-resources:
      base:
        paths:
          - "classpath:static"

datasources:
  default:
    url: jdbc:postgresql://localhost:5432/postgres
    driverClassName: org.postgresql.Driver

meta-server:
  cache.folder: "./cache"
  table.name: "servers"
  module:
    metadataFileNames:
      - module.json
      - module.txt

